;; Set the name of the scheme program being run
(setq scheme-program-name "mit-scheme")
;; setup the mit-scheme program
(setq MITSCHEME_LIBRARY_PATH
    "/Applications/mit-scheme.app/Contents/Resources")
