(require 'whitespace)
;; (setq whitespace-style '(lines-tail face space-mark tab-mark)
;;       whitespace-line-column 80)
;; (setq whitespace-style '(lines-tail face)
;;       whitespace-line-column 80)
;;(global-whitespace-mode 0)

